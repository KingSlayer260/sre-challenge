apiVersion: v1
kind: Service
metadata:
  name: sre-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: sre-rontend
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30000