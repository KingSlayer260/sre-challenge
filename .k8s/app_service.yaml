apiVersion: v1
kind: Service
metadata:
  name: sre-service
  labels:
    app: sre-frontend
spec:
  type: ClusterIP
  selector:
    app: sre-frontend
  ports:
    - port: 5000
      targetPort: 5000
      name: http